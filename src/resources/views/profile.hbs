<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>account Profile</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.7.0/dist/css/bootstrap.min.css">
</head>

<style>
    #message {
        padding: 10px;
        margin-top: 10px;
        border-radius: 5px;
        background-color: wheat;
    }
</style>

<body style="background-color: coral;">
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-6">
                <h2>Thông tin tài khoản</h2>
                <p><strong>Họ và tên:</strong> {{account.name}} </p>
                <p><strong>Email:</strong> {{account.email}} </p>
                <p><strong>Số điện thoại:</strong> {{account.phone}} </p>
                <p><strong>Vị trí:</strong> {{account.location}} </p>
            </div>
            <div class="col-md-6">
                <h2>Chỉnh sửa thông tin tài khoản</h2>
                <form id="editProfileForm" method="post" action="">
                    <div class="mb-3">
                        <label for="newName" class="form-label">Họ và tên</label>
                        <input type="text" class="form-control" name="newName" value="{{account.name}}">
                    </div>
                    <div class="mb-3">
                        <label for="newEmail" class="form-label">Email</label>
                        <input type="email" class="form-control" name="newEmail" value="{{account.email}}">
                    </div>
                    <div class="mb-3">
                        <label for="newPhone" class="form-label">Số điện thoại</label>
                        <input type="phone" class="form-control" name="newPhone" value="{{account.phone}}">
                    </div>
                    <div class="mb-3">
                        <label for="confirmPassword" class="form-label">Xác thực mật khẩu</label>
                        <input type="password" class="form-control" name="confirmPassword">
                    </div>
                    <button type="submit" class="btn btn-primary">Lưu thay đổi</button>
                </form>
                <div id="message" class="mt-3" style="display: none;">{{message}}</div>
            </div>
        </div>
    </div>
    
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.7.0/dist/js/bootstrap.min.js"></script>
</body>
</html>
<script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
<script>

    const messageDiv = document.getElementById('message');
    const form = document.getElementById('editProfileForm');

    if (messageDiv.innerText != "") {
        messageDiv.style.display = 'block';
        console.log(messageDiv.innerText);
    }

    // Kiểm tra nếu cả hai phần tử tồn tại
    if (messageDiv && form) {
        // Ẩn thông báo khi biểu mẫu được tương tác
        form.addEventListener('click', function () {
            messageDiv.style.display = 'none';
        });
    }

</script>
